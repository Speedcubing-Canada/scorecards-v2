runtime: nodejs24
entrypoint: node server.js

env_variables:
  NODE_ENV: production
  VITE_WCA_CLIENT_ID: 'XaOVOOv2h5PofsQ9ak_IYXJeDPCP69qagzF7WDdd03E'
  # WCA_CLIENT_SECRET must be set separately, do NOT put it here.
  # manage it via Cloud Secret Manager and load it at startup.

# CI + deployment pipeline.
#   push to any branch → lint, typecheck, test with coverage (no deploy)
#   pull request       → same
#   push to main       → same → build → deploy to App Engine
#
# There is no staging environment, so the test job is the only thing standing
# between a branch and production. It runs on every branch push, not just on
# PRs, so a branch is known-good before anyone opens one.
#
# See .github/workflows/README.md for one-time GCP setup instructions.
#
# Repository variables required (Settings → Secrets and variables → Variables):
#   WIF_PROVIDER        Workload Identity provider resource name
#   WIF_SA              Service account email for deploys
#   VITE_WCA_CLIENT_ID  WCA OAuth client ID

name: Deploy

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  PROJECT: scorecards-v2-prod

on:
  push:
  pull_request:
  workflow_dispatch:

# Cancel a run only when a newer commit supersedes it. The event is part of the key
# on purpose: a push run and a pull_request run of the SAME commit publish separate
# check contexts onto that commit, so cancelling one leaves the PR permanently red.
# Never cancel on main - that would kill a live deploy.
concurrency:
  group: ci-${{ github.event_name }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: read
  id-token: write

jobs:
  # ── 1. Lint, typecheck, test ──────────────────────────────────────────────
  # Runs on every push, and on pull requests from forks. A same-repo branch already
  # got a push run for the very same commit, and both events publish their own check
  # contexts onto it, so running again would only duplicate the work - but the
  # pull_request event is the ONLY one that ever fires for a fork, whose pushes land
  # in the contributor's repo and never reach ours. Hence: skip the duplicate, keep
  # the fork.
  #
  # The deploy job below is skipped unless the ref is main.
  test:
    name: Lint, typecheck & test
    runs-on: ubuntu-latest
    if: >-
      github.event_name != 'pull_request'
      || github.event.pull_request.head.repo.full_name != github.repository

    steps:
      - uses: actions/checkout@v7

      - name: Set up Node.js 24
        uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      # `npm run build` runs this too, but as its own step it typechecks src/**
      # (test files included) and fails a PR that never reaches a build.
      - name: Typecheck
        run: npm run typecheck

      # Fails if coverage drops below the thresholds committed in
      # vitest.config.ts. Raising them is automatic (thresholds.autoUpdate).
      - name: Test
        run: npm run test:coverage

      - name: Coverage summary
        if: always()
        run: |
          [ -f coverage/coverage-summary.json ] || exit 0
          jq -r '.total | "| metric | % | covered / total |",
                          "|---|---|---|",
                          "| lines | \(.lines.pct)% | \(.lines.covered) / \(.lines.total) |",
                          "| statements | \(.statements.pct)% | \(.statements.covered) / \(.statements.total) |",
                          "| functions | \(.functions.pct)% | \(.functions.covered) / \(.functions.total) |",
                          "| branches | \(.branches.pct)% | \(.branches.covered) / \(.branches.total) |"' \
            coverage/coverage-summary.json >> "$GITHUB_STEP_SUMMARY"

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage
          path: coverage/
          retention-days: 7

  # ── 2. Build and deploy ───────────────────────────────────────────────────
  deploy:
    name: Deploy → Production
    needs: test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Set up Node.js 24
        uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        env:
          VITE_WCA_CLIENT_ID: ${{ vars.VITE_WCA_CLIENT_ID }}
        run: npm run build

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ vars.WIF_PROVIDER }}
          service_account: ${{ vars.WIF_SA }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v3

      - name: Deploy to App Engine
        run: ./deploy.sh

  # ── 3. Clean up old versions (keep newest 5) ──────────────────────────────
  cleanup:
    name: Cleanup old versions
    needs: deploy
    runs-on: ubuntu-latest
    continue-on-error: true

    steps:
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ vars.WIF_PROVIDER }}
          service_account: ${{ vars.WIF_SA }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v3

      - name: Delete old versions (keep newest 5)
        env:
          KEEP: "5"
        run: |
          all=$(gcloud app versions list \
            --project="${PROJECT}" \
            --service="default" \
            --sort-by="~version.createTime" \
            --format="value(version.id)" 2>/dev/null || true)

          [ -z "${all}" ] && echo "No versions found - skipping" && exit 0

          to_delete=$(echo "${all}" | tail -n +$((KEEP + 1)))

          [ -z "${to_delete}" ] && \
            echo "$(echo "${all}" | wc -l | tr -d ' ') version(s) - nothing to remove" && \
            exit 0

          echo "Removing: $(echo "${to_delete}" | tr '\n' ' ')"
          while IFS= read -r v; do
            gcloud app versions delete "${v}" \
              --project="${PROJECT}" \
              --service="default" \
              --quiet \
              && echo "✓ deleted ${v}" \
              || echo "✗ could not delete ${v} (skipping)"
          done <<< "${to_delete}"
